# App Directory

This directory contains the main application code for the Exam Room Checker.

## Structure

- `api/`: API-related modules for data queries and sorting.
- `cli/`: Command-line interface scripts for PDF parsing and database searching.
- `helper/`: Utility modules for database operations, models, and PDF parsing.
- `web/`: Web interface components (future implementation).
